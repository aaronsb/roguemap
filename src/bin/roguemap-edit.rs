//! The asset editor (ADR-003). Not built yet: this stub keeps the binary
//! target in place so `cargo build` produces it.

fn main() {
    eprintln!("editor not built yet");
    std::process::exit(1);
}
